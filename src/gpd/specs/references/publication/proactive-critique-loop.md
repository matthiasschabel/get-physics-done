<purpose>Lazy write-paper critique gate.</purpose>

<critique_loop>
Project-backed only. Before staged peer review, ask `gpd-review-significance`
(`readonly=false`) "How can we improve this paper?" Skip if `paper_revision`
covered this round or budget is tight. Apply only actionable medium/high edits
with targeted `gpd-paper-writer`; checkpoint for new work. Log run/skip/failure
in `${PAPER_DIR}/CRITIQUE-LOG.md`.
</critique_loop>
